# 2026-07-07 工作日志

## 分析 OpenHarmony GitBook 文档
- 对 `E:\doc\gitbook\gitbook-openharmony` 整本 GitBook 做了量化分析（35 内容页）。
- 关键数据：中文字数 ≈27,000；图片 122 张且全部资源存在（无断图）；代码块仅 30 个且集中少数章节；外部链接 77；SUMMARY 命中率 100%。
- 主要结论：结构/导航/配图扎实（优势），但内容深度严重不均——约 2/3 章节仍为占位稿（21-XTS 239 字、12 图形系列、17/18 分布式、19 内核系列偏薄）；代码实例匮乏；13-窗口含大段 Windows 史离题；文件名命名不统一（06/09/20/21 等粘连）；SUMMARY 扁平而 mu-lu 嵌套，两套目录；图片用 GitBook Classic `<figure>` 私有语法有迁移风险。
- 产出：`OpenHarmony书籍分析报告.md` + `chapter_depth_chart.svg`（章节内容量条形图）。已清理临时分析脚本。

## 制定深度优化与内容扩充计划
- 基于分析报告，输出 `OpenHarmony书籍深度优化计划.md`：建立 L0–L3 成熟度模型（目标全书 8–10 万字），给出 35 篇现状分级与目标、统一章节模板（9 段结构）、Phase0–4 执行路线（Phase0 地基一致性→Phase1 核心子系统→Phase2 基础框架→Phase3 收尾→Phase4 审校）、质量门禁与协作方式（大纲确认两段式）。
- 待用户确认后逐章实施；未改动现有正文。

## 执行 Phase 0 地基改造（全部完成）
- 重命名 7 个不规范文件为 NN-topic.md（06/07/09/11/20/21 等粘连命名已统一）；同步改 SUMMARY/mu-lu 链接。
- SUMMARY 与 mu-lu 对 11（IPC+Binder）、12（图形 4 篇）、19（内核 4 篇）做缩进分组，两套目录已对齐一致。
- 全书 122 张 `<figure>` 私有图片语法迁移为标准 Markdown `![alt](../../.gitbook/assets/...)`，0 断图。
- 全 30 篇补充/修正 YAML frontmatter（description 取自 H1），修复了历史累积的重复 frontmatter 块 + 双重编码 BOM（ï»¿）问题，最终 0 异常。
- 全 30 篇末尾新增 `## 相关阅读` 互链小节（图形↔窗口↔输入、分布式软总线↔数据↔硬件、内核↔启动↔HDF、NAPI↔源码↔native 等）。
- 验证脚本全绿：30 篇全部在 SUMMARY、0 figure 残留、0 断图、0 命名不规范、互链全部可解析。
- 发现 14 张孤儿资源（.gitbook/assets 中未被任何 md 引用），未删除，待用户决定是否清理。
- 已清理全部临时脚本（phase0_*.py、_*.py）。

## 工具链注意事项（重要，易复犯）
- 本机 managed Python 运行环境在 2026-07-07 被重新解包：正确解释器路径为 `C:\Users\Administrator\.workbuddy\binaries\python\versions\3.13.12\python\python.exe`（内层 `python/` 目录才是真 home），外层 `versions\3.13.12\python.exe` 已失效（找不到 Lib）。
- `envs/default/Scripts/python.exe` 这个 venv 也已失效（指向旧 layout，报 "No module named encodings"）。
- Bash 环境中 `PYTHONPATH` 被设为 `D:\Program Files\WorkBuddy\...\cli\vendor\shim`，会污染 Python 导入；运行 python 前需 `PYTHONPATH=""` 或直接使用上面那个内层解释器。
- 用 Write 写 .py 文件再用内层解释器运行，比 heredoc 更稳（heredoc 易触发 env 异常）。

## GitBook 架构适配性评估与硬化（完成）
- 用户问"全部文档能适配 gitbook 架构么"。结论：**gitbook-cli（开源 legacy）完全适配**；GitBook.com SaaS 不兼容（需转纯 Markdown）。
- 关键澄清：正文中 `{% code %}`/`{% endcode %}`/`{% embed %}` 是 GitBook **原生标签**（非插件依赖），说明本书本就为 gitbook-cli 所写。
- 发现原图片存于 `.gitbook/assets/`（gitbook 的构建/缓存目录）——反模式，且 136 张被 git 跟踪，上次资源重命名还遗留 314 处脏 git 状态。
- 硬化动作：136 张图片迁至规范源目录 `assets/`（git 识别为 rename），30 篇 126 处引用改写；补 `.gitignore`（忽略 `.gitbook/`/`_book/`）；补最小 `book.json`（title/language=zh-hans/structure）。
- 最终校验全绿：30 章、110 张图片引用 0 断图、0 figure 残留、frontmatter 0 异常、互链可解析、配置齐备。
- 遗留：assets/ 内仍有 14 张孤儿图（未引用），可删；git 暂存 180 项改动（未提交，待用户确认）。
- 产出：`GitBook适配性报告.md`。

## 提交并推送（完成）
- 用户要求提交并 push。已 `git add -A` 后提交：`60d1ddc docs: Phase 0 地基改造 + GitBook 构建适配硬化`（181 files changed, +8297/-2122）。
- 提交内容含：7 个文件重命名、11/12/19 目录分组、122 图语法迁移、30 篇 frontmatter、30 篇相关阅读、136 图资源迁 assets/、`.gitignore`+`book.json`，以及 4 份报告/计划/图表文档。
- `git push -u origin main` 成功（ad1e889..60d1ddc），main 已 track origin/main，工作树干净。
- 注：仓库 remote 为 ssh://ssh.github.com:443/xxy-lanlanx/gitbook-openharmony.git（ssh 推送正常）。

## 缺口分析与缺失章节骨架（完成）
- 用户要求先不补全内容，先识别"针对 OpenHarmony 还缺哪些重要内容"并建立骨架。
- 缺口分析（对照官方架构）：原 22 章缺 安全子系统、权限访问控制、Ability 应用框架、包管理、编译构建、分布式任务调度、电源、通知事件、通信、位置、全球化、文件存储、帐号身份。
- 用生成脚本批量创建 13 个骨架文件（23-an-quan / 23-quan-xian / 24-ability / 25-bao / 26-build / 27-scheduler / 28-power / 29-notify / 30-connectivity / 31-location / 32-global / 33-filestorage / 34-account），每篇含 frontmatter(description)+定位+`>状态：骨架待补全`+规划###小节+相关阅读互链。
- 目录改造：SUMMARY/mu-lu 将 16/17/18 重组成"分布式能力"组并加入 27；22 后追加"安全与权限""应用框架""26 编译构建""系统基础服务"四组。两份目录已对齐。
- 验证全绿：13 文件均被收录、frontmatter 正常、相关阅读零断链、SUMMARY vs mu-lu 仅差 mu-lu 不自引用（原本即如此，正确）。
- 产出：`缺失章节骨架清单.md`（缺口表+13骨架规划+补全优先级 P0–P3）。
- 本轮未提交（用户未要求）；骨架待 review 后可提交。

## 补全 13 篇缺失章节骨架正文（完成）
- 用户要求"先补全新增的东西"：将缺口分析建立的 13 个骨架章节全部填充为详细正文。
- 采用统一模板（定位→架构/核心概念→关键流程→代码示例→常见问题→相关阅读→参考），移除"骨架待补全"标记。
- P0 核心 5 篇（L2–L3，含代码示例）：23-安全(HUKS/设备认证/签名)、23-权限(AccessToken/APL/沙箱)、24-Ability(FA vs Stage/Want/生命周期/跨设备)、25-包管理(HAP/安装/BundleManager)、26-编译构建(hb/GN/Ninja/BUILD.gn)。
- 27-分布式任务调度（跨设备启动/流转 ContinueAbility/协同/设备发现）。
- P2 系统基础服务 7 篇（扎实首通稿，含适用的代码）：28-电源、29-通知公共事件、30-通信、31-位置、32-全球化、33-文件存储、34-帐号身份。
- 验证：自写脚本确认 13 篇均无骨架残留、frontmatter 正常、相关阅读互链零断链（0 issue）。
- 修复一处隐藏字符链接 bug：33 章某链接 `23-quan-xian-guan-li-zi-tong.md` 漏写 `-xi-`（codepoint 级差异，肉眼与 repr 难辨），已修正为正确文件名。
- 更新 `缺失章节骨架清单.md` 状态为"已全部补全正文"。
- 本轮未提交（用户未要求）。注意：当前 git 工作树相对上次 push 已有 13 篇新内容（未提交）。

## 提交并推送 13 篇新章节（完成，含强推）
- 提交：`5f2150c docs: 补全13篇缺失子系统章节`（17 files changed, +1303/-6），含 13 篇新章节 + SUMMARY/mu-lu 扩展 + 缺失章节骨架清单.md + 工作日志。
- 普通 push 被拒：远端存在 `bd96037 GitBook: Update content`（GitBook 网页编辑器自动提交）。
- 核查：bd96037 把源图写回 `.gitbook/assets/`（132 张，反模式）、将章节重组成子文件夹 `xxx/README.md`、图片路径换回 `.gitbook/assets`；经 `git merge-tree` 预览仅 SUMMARY.md 冲突，对 01 章 diff 确认其改动仅为标题大小写/列表符号/图路径，**正文零新增**。
- 用户确认后执行 `git push --force-with-lease origin main` 成功（`bd96037...5f2150c forced update`），远端现为干净 gitbook-cli 结构 + 13 新章节，与本地同步。
- 教训已写入 MEMORY.md：GitBook 网页端会产生与本地冲突的提交，本项目以 gitbook-cli 为准，勿在网页端编辑；推送被拒先 fetch+merge-tree 预览，确无独特内容再用 `--force-with-lease` 覆盖。

## 清理 assets/ 14 张孤儿图（完成）
- 用户要求清理 assets/ 里的 14 张孤儿图。脚本全量扫描项目所有 .md 确认：136 张图中有 122 张被引用，14 张（约 2.4 MB）无任何引用（多为 GitBook 网页端导出遗留未接线图，如 image-14.png、渲染GPU架构图.png 等）。
- 用 `git rm` 删除 14 张并记录（未提交，留待下轮 push）。删除后 assets/ 剩 122 张，重新校验 0 孤儿、book 章节 0 断图。
- 注：脚本报 3 个 "broken refs" 均为误报——在 3 份分析/计划文档（不在 SUMMARY、不成书）里的 `![alt](../../.gitbook/assets/xxx.png)` / `![...](...)` 语法示意占位，非真实图片。
- MEMORY.md 中 "Phase 0 遗留：14 张孤儿图" 现已清理。

## Phase 1 核心子系统内容加深（进行中，本轮推进）
- 重跑字数统计，确认最薄章节，优先加深"占位/L1 级"核心子系统。
- 本轮深化的 7 篇（均插入实质内容至 `## 相关阅读` 之前，L2→L3）：
  - 21-xts-zi-xi-tong.md：补充 XTS 组成(acts/dcts/hts/tools)、acts 两级组织、JS/C++ 双测试框架、hb 编译执行、兼容性认证流程、示例 acts 用例。CJK 252→>770。
  - 07-napi-jie-kou-aki.md：补 AKI vs 原生 NAPI 对比表、数据类型映射、对象绑定 JSBIND_CLASS、线程安全、工程集成、适用限制。CJK 285→788。
  - 12-tu-xing-zi-xi-tong-openharmony.md：补轻量系统图形 UI 框架定位、渲染架构(软绘/硬加速)、2D 图形库、布局控件体系、事件交互。CJK 291→824。
  - 12-tu-xing-zi-xi-tong-linux-tu-xing-xian-shi-xi-tong.md：补五层图形栈总览、Wayland vs X11、EGL、KMS 四对象、OH 标准系统基于 DRM/KMS 自建显示路径。CJK 427→>770。
  - 19-nei-he-zi-xi-tong-qing-liang-xi-tong.md：补 LiteOS-M vs LiteOS-A 差异、内核基础功能集、KAL(CMSIS/POSIX)、启动流程、驱动与可选组件、适用场景。CJK 501→>770。
  - 02-kai-fa-huan-jing-da-jian-pian.md：补总体流程、repo 版本选择、hb 构建工具、prebuilts、系统类型环境差异、Docker、环境问题排查表。CJK 239→814。
  - 03-kai-fa-gong-ju-pian.md：补 DevEco Studio vs Device Tool 定位、hb、hdc 调试、hiperf/hitrace、烧录与串口、签名发布、推荐工作流表。CJK 305→765。
- 结论：原占位章节已消除；当前最薄者（32-全球化519 / 30-通信541 / 31-位置557 / 12-gpu602 / 29-通知610）均为此前 13 篇补全时填入的"代码丰富型 L2 首通稿"，结构完整、非占位，可视为达标。
- 临时统计脚本 `_stats_now.py` 保留在仓库根（untracked），便于后续复查字数。
- 本轮未提交；14 张孤儿图 `git rm` 仍处 staged 状态，等待下一轮 push。

